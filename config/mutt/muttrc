source ~/.config/mutt/colors

set folder		= "imaps://imap-mail.outlook.com"
set imap_user	= "berkin_akkaya@hotmail.com"
set imap_pass	= "Ber34kin60"
set smtp_url	= "smptps://smtp-mail.outlook.com"
set smtp_pass	= "Ber34kin60"
set spoolfile	= +INBOX
mailboxes		= +INBOX

set editor		= "nvim"

set header_cache		= "~/.config/mutt/cache"
set message_cachedir	= "~/.config/mutt/cache"

# Allow Mutt to open a new IMAP connection automatically.
unset imap_passive
